module.exports = {
    plugins: [
        require('tailwindcss'), // This plugin processes Tailwind CSS directives
        require('autoprefixer'), // This plugin adds vendor prefixes to CSS
        // You can add other PostCSS plugins here if needed
    ]
}
